import { TimerColor } from './timer-color';

describe('TimerColor', () => {
  it('should create an instance', () => {
    const directive = new TimerColor();
    expect(directive).toBeTruthy();
  });
});
