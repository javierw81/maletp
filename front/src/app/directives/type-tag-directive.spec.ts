import { TypeTagDirective } from './type-tag-directive';

describe('TypeTagDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeTagDirective();
    expect(directive).toBeTruthy();
  });
});
