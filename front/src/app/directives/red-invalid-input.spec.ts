import { RedInvalidInput } from './red-invalid-input';

describe('RedInvalidInput', () => {
  it('should create an instance', () => {
    const directive = new RedInvalidInput();
    expect(directive).toBeTruthy();
  });
});
